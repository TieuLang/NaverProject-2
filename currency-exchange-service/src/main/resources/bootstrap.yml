spring:
  application:
    name: currency-exchange-service
  profiles:
    active: dev
  cloud:
    config: http://localhost:8888
    enabled: true

server:
  port: 8000

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka